Краткий вывод
IMyCloneable

Преимущества:

типобезопасность;
не требуется приведение типов;
удобно использовать в generic-коде.

Недостатки:

пользовательский интерфейс;
не является частью стандартной библиотеки .NET.
ICloneable

Преимущества:

стандартный интерфейс .NET;
поддерживается многими библиотеками.

Недостатки:

метод Clone() возвращает object;
требуется cast;
не определяет тип копирования (глубокое или поверхностное).